package com.cp.weektwo.challenges.challenge3;

public class TimeAndSpaceAnalysisForOneAndTwo {
	/**
	 * 
	 * What is the time and space complexity of the 
	 * LinkedListNode.setValuesFromArray method in 
	 * Challenge #1?
	 * 
	 * (N) nodes and have to iterate through N nodes.
	 * O(N) space
	 * O(N) time
	 * 
	 * What is the time and space complexity of addition algorithm in Challenge #2?
	 * N list1 + M list2
	 * 
	 * Time complexity is O(n+m) because we have to iterate through both lists
	 * Space complexity Math.max(N, M)
	 * 
	 */
}
